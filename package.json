{
  "name": "jest-ajv",
  "version": "1.0.0",
  "description": "JSON schema matcher for jest using ajv",
  "main": "index.js",
  "scripts": {
    "lint": "eslint ./ --ignore-path .gitignore --ext .js",
    "pretest": "npm run lint",
    "test": "jest"
  },
  "repository": {
    "type": "git",
    "url": "ssh:****.com/uie/jest-ajv.git"
  },
  "jest": {
    "preset": "one-amex-jest-preset"
  },
  "keywords": [
    "jest",
    "ajv",
    "json",
    "schema",
    "matcher",
    "test"
  ],
  "author": "Jimmy King <jimmy.king@aexp.com> (https://github.com/jking90)",
  "license": "Apache-2.0",
  "dependencies": {
    "ajv": "^4.11.5",
    "chalk": "^1.1.3",
    "jest-matcher-utils": "^19.0.0"
  },
  "devDependencies": {
    "eslint": "^3.18.0",
    "eslint-config-one-amex": "^3.0.0",
    "jest": "^19.0.2",
    "one-amex-jest-preset": "^2.1.0"
  }
}
